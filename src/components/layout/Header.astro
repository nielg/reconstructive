---
import Nav from "./Nav.astro";

interface Props {
  pathname: string;
}
const { pathname } = Astro.props;
---

<header class="bg-white">
  <h2 class="text-green center">Reconstructive.be</h2>
  <Nav pathname={pathname} />
</header>

<style>
  header {
    padding-top: 3rem;
  }
  h2 {
    color: var(--reconstructive-green);
    font-size: 3.5rem;
  }
</style>
